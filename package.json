{
  "scripts": {
    "test": "browserify index.js -t [ babelify ] -p [ split-require --dir dist ] -o dist/index.js"
  },
  "devDependencies": {
    "babel-core": "^6.26.0",
    "babel-plugin-dynamic-import-split-require": "^1.0.0",
    "babel-plugin-transform-react-jsx": "^6.24.1",
    "babel-preset-env": "^1.6.1",
    "babelify": "^8.0.0",
    "browserify": "^14.5.0",
    "split-require": "^2.0.2"
  }
}
